apiVersion: v1
kind: Service
metadata:
  name: {{ include "parodos-testing.fullname" . }}-workflow-service
  labels:
  {{- include "parodos-testing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.workflowService.type }}
  selector:
    app: workflow-service
  {{- include "parodos-testing.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.workflowService.ports | toYaml | nindent 2 -}}